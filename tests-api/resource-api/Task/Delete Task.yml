info:
  name: Delete Task
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base-url}}/api/tasks/{{task-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
