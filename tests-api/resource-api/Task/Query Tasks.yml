info:
  name: Query Tasks
  type: http
  seq: 1

http:
  method: GET
  url: "{{base-url}}/api/tasks?start_to[lte]=2026-3-5"
  params:
    - name: start_to[lte]
      value: 2026-3-5
      type: query
    - name: limit
      value: "5"
      type: query
      disabled: true
    - name: page
      value: "1"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
