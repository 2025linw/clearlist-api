info:
  name: Create Task Tag
  type: http
  seq: 3

http:
  method: POST
  url: "{{base-url}}/api/tasks/{{task-id}}/tags/{{tag-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
