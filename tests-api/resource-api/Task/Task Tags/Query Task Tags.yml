info:
  name: Query Task Tags
  type: http
  seq: 1

http:
  method: GET
  url: "{{base-url}}/api/tasks/{{task-id}}/tags"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
