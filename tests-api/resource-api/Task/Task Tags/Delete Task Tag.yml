info:
  name: Delete Task Tag
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base-url}}/api/tasks/{{task-id}}/tags/{{tag-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
