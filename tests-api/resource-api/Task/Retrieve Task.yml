info:
  name: Retrieve Task
  type: http
  seq: 3

http:
  method: GET
  url: "{{base-url}}/api/tasks/{{task-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
