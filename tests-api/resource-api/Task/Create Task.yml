info:
  name: Create Task
  type: http
  seq: 2

http:
  method: POST
  url: "{{base-url}}/api/tasks"
  body:
    type: json
    data: |-
      {
        "title": "Test Title",
        "notes": "This is a sample note for 'Test Title'",
      //   "startDate": "2027-01-01",
        "startAt": "2027-01-01T19:00:00.000Z",
        "deadline": "2027-2-1"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status === 201) {
          let body = res.body;
          
          bru.setVar("task-id", body.data.taskId)
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
