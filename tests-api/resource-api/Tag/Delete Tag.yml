info:
  name: Delete Tag
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base-url}}/api/tags/{{tag-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
