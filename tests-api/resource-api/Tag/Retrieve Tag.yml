info:
  name: Retrieve Tag
  type: http
  seq: 3

http:
  method: GET
  url: "{{base-url}}/api/tags/{{tag-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
