info:
  name: Get Session
  type: http
  seq: 2

http:
  method: GET
  url: "{{base-url}}/api/auth/get-session"
  headers:
    - name: Origin
      value: https://todo.saphynet.io
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
