info:
  name: Signout
  type: http
  seq: 4

http:
  method: POST
  url: "{{base-url}}/api/auth/sign-out"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
