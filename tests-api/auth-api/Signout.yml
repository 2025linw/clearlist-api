info:
  name: Signout
  type: http
  seq: 3

http:
  method: POST
  url: "{{base-url}}/api/auth/sign-out"
  headers:
    - name: Origin
      value: https://todo.saphynet.io
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
